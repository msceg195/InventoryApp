<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Padding="20"  Title="Select Requesitions..."
             x:Class="DroidInventory_MSC.MaterialIssueRequesitionsPage">
    <ListView x:Name="listMaterialRequesitions"  HasUnevenRows="True" SeparatorVisibility="Default" SeparatorColor="#fecb02"   >
        <ListView.ItemTemplate >
            <DataTemplate>
              
                <ViewCell>
                    
                    <StackLayout Orientation="Vertical">
                        <Label Text="{Binding RName}" TextColor="Black" />
                        <Label Text="{Binding EnglishName}"  />
                        <Label Text="{Binding RequesitionDate}"  />
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
            
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>